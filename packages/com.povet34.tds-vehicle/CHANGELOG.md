# Changelog

## [0.2.0]
- `CarRam`: pure ramming-damage scaling — `CanDamage(speed, minSpeed)` and
  `DamageAt(speed, minSpeed, base, max)` (damage grows with speed, capped).

## [0.1.0]
- Initial extraction from EscapeFromDesertPlanet TDS.Core.
